name: Build ZMK firmware
on: [push, workflow_dispatch]


jobs:
  build:
    uses: Nuclear-Squid/zmk/.github/workflows/build-user-config.yml@perf/faster-ci
    with:
      archive_name: firmware-selenium
